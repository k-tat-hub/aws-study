name: Terraform CI/CD

on:
  push:

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5
      - run: echo "Hello GitHub Actions"
      - run: uname -a
      - run: ls -la